server:
  port: 8000

security:
  jwt:  
    secretKey: JwtSecretKey
    tokenPrefix: Bearer 
    header: Authorization
    expiration: 86400
    url: /auth/**

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/